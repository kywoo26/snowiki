default_install_hook_types: [pre-commit]

repos:
  - repo: local
    hooks:
      - id: ignored-artifact-guard
        name: ignored artifact guard
        entry: uv run python scripts/check_ignored_artifacts.py
        language: system
        pass_filenames: false
        always_run: true
